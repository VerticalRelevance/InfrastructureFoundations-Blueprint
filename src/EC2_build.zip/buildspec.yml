version: 0.2
  
phases:
  build:
    commands:
      - aws cloudformation create-stack --template-url s3://hashicorptestingbucket/EC2_build.zip/src/main/cft/EC2CFT.yml --stack-name jacob-ec2-test --capabilities CAPABILITY_IAM
               
reports:
  report-group-name-or-arn:
    reports_group:
      files:
        - my-test-report-file.json
# artifacts:
#   files:
#     - location
#     - location
#   name: artifact-name
#   discard-paths: no | yes
#   base-directory: location
#   exclude-paths: excluded paths
#   enable-symlinks: no | yes
#   s3-prefix: prefix
#   secondary-artifacts:
#     artifactIdentifier:
#       files:
#         - location
#         - location
#       name: secondary-artifact-name
#       discard-paths: no | yes
#       base-directory: location
#     artifactIdentifier:
#       files:
#         - location
#         - location
#       discard-paths: no | yes
#       base-directory: location

